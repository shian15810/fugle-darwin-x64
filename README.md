# fugle-darwin-x64

```bash
$ nativefier "https://www.fugle.tw/?p=darwin&v=${FUGLE_APP_DESKTOP_VERSION}" --name "Fugle" --inject "./fugle-app-prerequisite/not-for-app.css" --icon "./fugle-app-prerequisite/fugle.png" --platform "darwin" --arch "x64"
```
